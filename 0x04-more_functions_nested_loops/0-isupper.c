#include "main.h"

/**
 * _isupper - checks if the character is uppercase
 * @c: Variable text
 * Return: Always 0
 */
int _isupper(char c)
{
	if (c >= 'A'; && c <= 'Z';)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
